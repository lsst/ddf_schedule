from setuptools import setup

setup(
    name='ddf_schedule',
    version='0.1',
    description='Pull the current Deep Drilling Field schedule',
    packages=['ddf_schedule'],
    install_requires=[
        'numpy',
        ],
)
